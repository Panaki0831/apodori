{
  "name": "@sales-ai/prisma",
  "version": "1.0.0",
  "private": true,
  "main": "./generated/client/index.js",
  "types": "./generated/client/index.d.ts",
  "scripts": {
    "generate": "npx dotenv -e ../.env -- prisma generate",
    "push": "npx dotenv -e ../.env -- prisma db push",
    "migrate": "npx dotenv -e ../.env -- prisma migrate dev",
    "build": "npx dotenv -e ../.env -- prisma generate",
    "clean": "rm -rf generated",
    "lint": "echo 'lint ok'"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "prisma": "^6.2.0"
  }
}
